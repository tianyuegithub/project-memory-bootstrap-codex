# Project Memory Events - 2026-04-27

## 事件 1: 修复 symlink 写入逃逸风险并收窄 CI 权限
- 类别: verification
- 来源: `src/project_memory_bootstrap_codex/bootstrap.py`; `.github/workflows/test.yml`; `tests/test_bootstrap.py`; `README.md`
- 验证命令/证据: `PYTHONPATH=src python3 -m unittest discover -s tests`
- 影响范围: repo, module:cli, module:ci
- 是否可晋升: yes
- 记录:
  - `write_if_needed()` 写入前会解析目标路径和父目录路径，拒绝写入或创建项目根目录外的路径。
  - 补充了直接文件 symlink 和父目录 symlink 两类回归测试。
  - GitHub Actions 顶层声明 `permissions: contents: read`，按当前测试用途收窄 token 权限。
  - README 新增安全说明，解释 CLI 不执行脚本、不访问网络，以及开源前需要审查 `.codex/memory/*`。

## 事件 2: README 增加中英文介绍视频
- 类别: migration
- 来源: `README.md`; `docs/videos/project-memory-bootstrap-codex-intro-zh.mp4`; `docs/videos/project-memory-bootstrap-codex-intro-en.mp4`; `docs/videos/project-memory-bootstrap-codex-intro-zh.png`; `docs/videos/project-memory-bootstrap-codex-intro-en.png`
- 验证命令/证据: `ffprobe -v error -select_streams v:0 -show_entries stream=width,height,r_frame_rate,nb_frames,duration -show_entries format=duration,size -of json`; HyperFrames 中文版 `lint`/`inspect` 通过；中英文视频均已本地抽帧查看；GitHub README 渲染会过滤 `<video>` 标签，因此改为封面图链接到 MP4 文件页
- 影响范围: repo, docs, readme-media
- 是否可晋升: yes
- 记录:
  - README 新增“视频介绍”章节，使用封面图链接到中英文 MP4，点击封面后在 GitHub MP4 文件页播放。
  - 中文版视频 `docs/videos/project-memory-bootstrap-codex-intro-zh.mp4` 为 1920x1080, 24fps, 32 秒，结尾突出 `@tianyuegithub`。
  - 英文版视频 `docs/videos/project-memory-bootstrap-codex-intro-en.mp4` 为 1920x1080, 24fps, 32 秒。
  - 视频文件用于 GitHub README 展示，不参与 CLI 运行链路。

# Project Memory Events - 2026-04-27

## 事件 1: 修复 symlink 写入逃逸风险并收窄 CI 权限
- 类别: verification
- 来源: `src/project_memory_bootstrap_codex/bootstrap.py`; `.github/workflows/test.yml`; `tests/test_bootstrap.py`; `README.md`
- 验证命令/证据: `PYTHONPATH=src python3 -m unittest discover -s tests`
- 影响范围: repo, module:cli, module:ci
- 是否可晋升: yes
- 记录:
  - `write_if_needed()` 写入前会解析目标路径和父目录路径，拒绝写入或创建项目根目录外的路径。
  - 补充了直接文件 symlink 和父目录 symlink 两类回归测试。
  - GitHub Actions 顶层声明 `permissions: contents: read`，按当前测试用途收窄 token 权限。
  - README 新增安全说明，解释 CLI 不执行脚本、不访问网络，以及开源前需要审查 `.codex/memory/*`。

## 事件 2: README 增加中英文介绍视频
- 类别: migration
- 来源: `README.md`; `docs/videos/project-memory-bootstrap-codex-intro-zh.mp4`; `docs/videos/project-memory-bootstrap-codex-intro-en.mp4`; `docs/videos/project-memory-bootstrap-codex-intro-zh.png`; `docs/videos/project-memory-bootstrap-codex-intro-en.png`
- 验证命令/证据: `ffprobe -v error -select_streams v:0 -show_entries stream=width,height,r_frame_rate,nb_frames,duration -show_entries format=duration,size -of json`; HyperFrames 中文版 `lint`/`inspect` 通过；中英文视频均已本地抽帧查看；GitHub README 渲染会过滤 `<video>` 标签，普通 blob MP4 页也不直接预览，GitHub Raw 返回 `application/octet-stream`，因此改为封面图链接到 jsDelivr 的 `video/mp4` 播放地址
- 影响范围: repo, docs, readme-media
- 是否可晋升: yes
- 记录:
  - README 新增“视频介绍”章节，使用封面图链接到中英文 jsDelivr MP4，点击封面后由浏览器直接播放或打开视频；源文件仍保存在 GitHub 仓库内。
  - 中文版视频 `docs/videos/project-memory-bootstrap-codex-intro-zh.mp4` 为 1920x1080, 24fps, 32 秒，结尾突出 `@tianyuegithub`。
  - 英文版视频 `docs/videos/project-memory-bootstrap-codex-intro-en.mp4` 为 1920x1080, 24fps, 32 秒。
  - 视频文件用于 GitHub README 展示，不参与 CLI 运行链路。

## 事件 3: README 中英文介绍视频增加配音讲解
- 类别: migration
- 来源: `README.md`; `docs/videos/project-memory-bootstrap-codex-intro-zh.mp4`; `docs/videos/project-memory-bootstrap-codex-intro-en.mp4`
- 验证命令/证据: HyperFrames 中英文项目 `lint --verbose` 与 `inspect --samples 14 --json` 通过；`hyperframes render --quality draft --fps 24 --workers 1` 重新渲染；`ffprobe -v error -show_streams -show_format` 确认两个 MP4 均包含 32 秒 AAC 音频流；`ffmpeg -af volumedetect` 确认音频非静音
- 影响范围: repo, docs, readme-media
- 是否可晋升: yes
- 记录:
  - 中文版视频使用中文配音讲解项目级记忆治理、`CODEX.md`、`MEMORY.md`、日期文件、`init`/`doctor` 用法，并在结尾突出 `github.com/tianyuegithub/project-memory-bootstrap-codex` 与 `@tianyuegithub`。
  - 英文版视频使用英文配音讲解相同产品定位与使用路径。
  - README 媒体章节已标明中文版为“带配音”，英文版为“with voiceover”。
  - README 的 jsDelivr 播放链接固定到包含有声视频资产的提交 `3b418625943a8278e64280649e1317b37a671307`，避免 `@main` 分支缓存短期返回旧视频。

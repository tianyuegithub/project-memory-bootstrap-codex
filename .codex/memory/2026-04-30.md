# Project Memory Events - 2026-04-30

## 事件 1: 增加低 token 运行时记忆索引层
- 类别: verification
- 来源: `src/project_memory_bootstrap_codex/memory_index.py`; `src/project_memory_bootstrap_codex/cli.py`; `tests/test_memory_index.py`; `README.md`; `docs/governance-v2.md`; `docs/adoption-guide.md`; `skills/project-memory-bootstrap-codex/SKILL.md`; `CODEX.md`
- 验证命令/证据: `PYTHONPATH=src python3 -m unittest discover -s tests`; `PYTHONPATH=src python3 -m project_memory_bootstrap_codex.cli index-memory .`; `PYTHONPATH=src python3 -m project_memory_bootstrap_codex.cli search-memory . "启动端口" --limit 5`; `PYTHONPATH=src python3 -m project_memory_bootstrap_codex.cli context-memory . "token 索引" --max-tokens 240`
- 影响范围: repo, module:cli, docs, skill
- 是否可晋升: yes
- 记录:
  - 新增 `memory_index.py`，提供零依赖 SQLite 本地索引，默认索引 `CODEX.md`、agent 上下文文件、`.codex/memory/*.md` 和 legacy memory 文件。
  - CLI 新增 `index-memory`、`search-memory`、`context-memory`，支持先查轻量索引，再按需生成预算受控上下文。
  - 中文查询增加 2 字滑窗匹配，避免“启动端口”与“运行入口与端口”这类近似表达漏检。
  - `.codex/cache/` 已加入 `.gitignore`，索引数据库作为本地缓存处理。

## 事件 2: 补齐按 ID 取回与索引新鲜度检查
- 类别: verification
- 来源: `src/project_memory_bootstrap_codex/memory_index.py`; `src/project_memory_bootstrap_codex/cli.py`; `tests/test_memory_index.py`; `README.md`; `docs/governance-v2.md`; `docs/adoption-guide.md`; `skills/project-memory-bootstrap-codex/SKILL.md`; `CODEX.md`; `.codex/memory/MEMORY.md`
- 验证命令/证据: `PYTHONPATH=src python3 -m unittest tests.test_memory_index`; `PYTHONPATH=src python3 -m unittest discover -s tests`; `PYTHONPATH=src python3 -m project_memory_bootstrap_codex.cli index-memory .`; `PYTHONPATH=src python3 -m project_memory_bootstrap_codex.cli memory-status .`; `PYTHONPATH=src python3 -m project_memory_bootstrap_codex.cli get-memory . 2d73dcb3560f`; `PYTHONPATH=src python3 -m project_memory_bootstrap_codex.cli context-memory . --id 2d73dcb3560f --max-tokens 200`
- 影响范围: repo, module:cli, docs, skill
- 是否可晋升: yes
- 记录:
  - 新增 `get_memory_by_id`、`index_status`、`context_from_ids`，形成 `search-memory -> get-memory/context-memory --id` 的精确展开链路。
  - 索引记录源文件内容哈希，`memory-status` 可发现源记忆文件新增、删除或内容变化。
  - `context-memory` 支持重复传入 `--id`，用于跳过二次搜索并生成预算受控上下文。
